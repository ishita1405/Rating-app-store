const express = require('express');
const router = express.Router();

// This is a placeholder for user-specific routes
// Currently, most user functionality is handled through auth routes

module.exports = router;
